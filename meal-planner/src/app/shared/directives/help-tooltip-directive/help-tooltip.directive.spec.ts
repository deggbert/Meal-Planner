import { HelpTooltipDirective } from './help-tooltip.directive';

describe('HelpTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new HelpTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
