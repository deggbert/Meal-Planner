import { UnitsDirective } from './units.directive';

describe('UnitsDirective', () => {
  it('should create an instance', () => {
    const directive = new UnitsDirective();
    expect(directive).toBeTruthy();
  });
});
